{
  "name": "localization",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "twilio-run",
    "deploy": "twilio-run deploy"
  },
  "dependencies": {
    "twilio": "^3.56",
    "twilio-flex-token-validator": "^1.5.6"
  },
  "devDependencies": {
    "twilio-run": "^2.6.0"
  }
}
